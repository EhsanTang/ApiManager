/**
 * 接口相关非数据交互方法
 * 后端代码
 */
var interfaceMethods = angular.module("interfaceMethods", []);


interfaceMethods.controller('interfaceMethods', function($rootScope,$scope, $http, $state, $stateParams,httpService) {
	$scope.getData = function(page,setPwd) {
		
    };
  });



